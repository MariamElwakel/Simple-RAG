# 📚 Simple Arabic RAG App

A simple Retrieval Augmented Generation (RAG) app built with Streamlit, LangChain, and ChromaDB to answer questions from an Arabic text document.

---

## 🚀 Setup

### 1️⃣ Create Virtual Environment

```bash
python -m venv venv
```

Activate it:

```bash
venv\Scripts\activate
```

---

### 2️⃣ Install Requirements

```bash
pip install -r requirements.txt
```

---

### 3️⃣ Add OpenAI API Key

Create a `.env` file in the project root:

```
OPENAI_API_KEY=your_api_key_here
```

---

## ▶️ Run the App

```bash
streamlit run app.py
```

---

## 📂 Files

- `app.py` → Main Streamlit RAG app  
- `arabic.txt` → Arabic document used for retrieval  
- `requirements.txt` → Dependencies  
- `Test Cases.txt` → Sample questions for testing  

---
